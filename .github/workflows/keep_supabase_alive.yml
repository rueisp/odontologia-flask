name: Keep Supabase Project Alive

on:
  schedule:
    - cron: '0 0 * * 0,3' # Corre a medianoche UTC los Domingos y Miércoles

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Flask App
        run: curl -s "https://TU_URL_DE_APLICACION_FLASK_AQUI.com/" # Reemplaza esta URL con la de tu app!